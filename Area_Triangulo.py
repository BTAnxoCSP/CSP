
print("--- Cálculo de Área do Triángulo ---")

base = float(input("Introduce a base (cm): "))
altura = float(input("Introduce a altura (cm): "))

area = (base * altura) / 2

print(f"\nO área do triángulo é de {area:.2f} cm².")